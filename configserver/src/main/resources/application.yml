spring:
  application:
    name: configserver
  profiles:
    active: native    #because using file present on local system
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

server:
  port: 8888